version: '3.2'
services:
    db:
        image: mysql:8.0
        container_name: db-brainly
        restart: always
        ports:
            - ${MYSQL_DB_PORT}:3306
        environment:
            MYSQL_ROOT_PASSWORD: ${MYSQL_DB_PASSWORD}
